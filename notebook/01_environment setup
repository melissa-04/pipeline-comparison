
from google.colab import drive
drive.mount('/content/drive')

print("✅ Google Drive bağlandı!")
import os

# === DRIVE (kalıcı depolama) ===
drive_base = "/content/drive/MyDrive/NGS_Project"

drive_dirs = [
    f"{drive_base}/results/qc",           # Kalite kontrol raporları
    f"{drive_base}/results/vcf",           # Variant calling sonuçları (küçük, ~MB)
    f"{drive_base}/results/comparison",    # Karşılaştırma metrikleri ve tablolar
    f"{drive_base}/results/figures",       # Grafikler ve görseller
    f"{drive_base}/logs",                  # İşlem logları
]

for d in drive_dirs:
    os.makedirs(d, exist_ok=True)
    print(f" {d}")

# === COLAB GEÇİCİ DİSK (büyük dosyalar için) ===
local_base = "/content/ngs_project"

local_dirs = [
    f"{local_base}/raw_data",             # FASTQ dosyaları (~20 GB)
    f"{local_base}/reference",            # Referans genom (~3 GB)
    f"{local_base}/bam",                  # BAM dosyaları (~15-30 GB)
    f"{local_base}/vcf",                  # Geçici VCF dosyaları
    f"{local_base}/ground_truth",         # Doğru cevap seti
]

for d in local_dirs:
    os.makedirs(d, exist_ok=True)
    print(f" {d}")

print("\n✅ Tüm klasörler oluşturuldu!")

